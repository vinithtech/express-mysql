const config = {
  port: 3000,
  mysql: {
    host: "localhost",
    user: "root",
    password: "password",
    database: "aadhar"
  },
  base_doman: "http://localhost:3000"
};

module.exports = config;
